# Não Ligar — Exclusão Pontual e Blacklist Permanente

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar checkboxes no modal de preview das campanhas para excluir contatos pontualmente, e botão "🚫 Nunca ligar" que persiste a exclusão permanentemente na blacklist.

**Architecture:** Migração cria `nao_ligar_pacientes` (tabela separada de `pacientes_abc` para sobreviver ao sync Clinicorp) e coluna `nao_ligar` em `leads`. `buscarContatos()` filtra blacklist automaticamente. Modal recebe checkboxes com estado local (Set de IDs desmarcados) e envia `excluir: [ids]` ao confirmar. "Nunca ligar" chama `POST /api/campanhas/nao-ligar` inline antes de fechar.

**Tech Stack:** Node.js + Express, Supabase Postgres (MCP), HTML/CSS/JS vanilla.

---

## File Structure

**Criar:**
- `supabase/migrations/20260601000003_nao_ligar.sql`

**Modificar:**
- `server.js` — endpoint novo, `buscarContatos`, `POST /api/campanhas/lancar`
- `public/js/pos-tratamento/curva-abc.js` — reescrever `_abrirModalCampanha`
- `public/index.html` — reescrever `_abrirModalCampanhaLeads`

---

## Task 1: DB Migration

**Files:**
- Create: `supabase/migrations/20260601000003_nao_ligar.sql`

Context: Projeto Supabase ID = `mtqdpjhhqzvuklnlfpvi`. Aplicar via `mcp__plugin_supabase_supabase__apply_migration`. O padrão de roles usado nas políticas RLS deste projeto é `(auth.jwt() -> 'user_metadata' ->> 'role') IN (...)` — verificar nas migrations existentes (ex: `20260529000000_campanhas_discagem.sql`) para confirmar antes de aplicar.

- [ ] **Step 1: Criar arquivo de migração**

```sql
-- supabase/migrations/20260601000003_nao_ligar.sql

CREATE TABLE nao_ligar_pacientes (
  clinicorp_id TEXT PRIMARY KEY,
  criado_em    TIMESTAMPTZ DEFAULT NOW()
);

ALTER TABLE nao_ligar_pacientes ENABLE ROW LEVEL SECURITY;

CREATE POLICY "nlp_select" ON nao_ligar_pacientes FOR SELECT
  USING ((auth.jwt() -> 'user_metadata' ->> 'role') IN ('crc_leads','crc_comercial','gestor','admin'));

CREATE POLICY "nlp_insert" ON nao_ligar_pacientes FOR INSERT
  WITH CHECK ((auth.jwt() -> 'user_metadata' ->> 'role') IN ('crc_leads','crc_comercial','gestor','admin'));

CREATE POLICY "nlp_delete" ON nao_ligar_pacientes FOR DELETE
  USING ((auth.jwt() -> 'user_metadata' ->> 'role') IN ('gestor','admin'));

ALTER TABLE leads ADD COLUMN nao_ligar BOOLEAN NOT NULL DEFAULT false;
```

- [ ] **Step 2: Aplicar migração via MCP**

Usar `mcp__plugin_supabase_supabase__apply_migration` com:
- `project_id`: `mtqdpjhhqzvuklnlfpvi`
- `name`: `nao_ligar`
- `query`: conteúdo do arquivo acima

- [ ] **Step 3: Verificar**

Usar `mcp__plugin_supabase_supabase__list_migrations` para confirmar que `nao_ligar` aparece na lista.

Usar `mcp__plugin_supabase_supabase__execute_sql` para confirmar:
```sql
SELECT column_name FROM information_schema.columns
WHERE table_name = 'leads' AND column_name = 'nao_ligar';
```
Esperado: retorna 1 linha.

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/20260601000003_nao_ligar.sql
git commit -m "feat: add nao_ligar_pacientes table and leads.nao_ligar column"
```

---

## Task 2: Backend — Endpoint + buscarContatos + lancar

**Files:**
- Modify: `server.js`

Context: `buscarContatos` está nas linhas 736–775. O endpoint `POST /api/campanhas/lancar` está nas linhas 789–842. O novo endpoint `POST /api/campanhas/nao-ligar` deve ser adicionado **antes** do bloco de endpoints de campanha existentes (antes da linha com `app.post('/api/campanhas/lancar'`). O middleware `requireCrcLead` já está definido no arquivo.

- [ ] **Step 1: Adicionar endpoint `POST /api/campanhas/nao-ligar`**

Localizar no `server.js` a linha:
```js
app.post('/api/campanhas/lancar', requireAuth, requireCrcLead, async (req, res) => {
```

Inserir **antes** dela:

```js
app.post('/api/campanhas/nao-ligar', requireAuth, requireCrcLead, async (req, res) => {
  try {
    const { tipo, id } = req.body;
    if (!tipo || !id) return res.status(400).json({ error: 'tipo e id são obrigatórios' });
    if (!['paciente', 'lead'].includes(tipo)) return res.status(400).json({ error: 'tipo deve ser "paciente" ou "lead"' });
    if (tipo === 'paciente') {
      const { error } = await supabase.from('nao_ligar_pacientes').insert({ clinicorp_id: String(id) });
      if (error && error.code !== '23505') throw error; // 23505 = unique violation (já bloqueado — OK)
    } else {
      const { error } = await supabase.from('leads').update({ nao_ligar: true }).eq('id', id);
      if (error) throw error;
    }
    res.json({ ok: true });
  } catch (e) {
    res.status(e.status || 500).json({ error: e.message });
  }
});
```

- [ ] **Step 2: Reescrever `buscarContatos` para filtrar blacklist**

Localizar e substituir a função `buscarContatos` completa (linhas 736–775):

```js
async function buscarContatos(tipo) {
  if (tipo === 'abc') {
    const { data: bloqueados, error: blqError } = await supabase.from('nao_ligar_pacientes').select('clinicorp_id');
    if (blqError) throw blqError; // falha aberta incluiria pacientes bloqueados na campanha
    const bloqueadosSet = new Set((bloqueados || []).map(r => String(r.clinicorp_id)));
    const { data, error } = await supabase.from('pacientes_abc')
      .select('nome, telefone, clinicorp_id, dias_sem_visita')
      .in('classe', ['A', 'B'])
      .gte('dias_sem_visita', 180)
      .is('proxima_consulta', null);
    if (error) throw error;
    return (data || [])
      .filter(p => !bloqueadosSet.has(String(p.clinicorp_id)))
      .map(c => ({ ...c, tipo_origem: 'abc' }));
  }
  if (tipo === 'indicacoes') {
    const { data, error } = await supabase.from('leads')
      .select('id, nome, telefone')
      .eq('origem', 'Indicação')
      .not('status', 'in', '("Fechou","Perdido")')
      .eq('nao_ligar', false);
    if (error) throw error;
    return (data || []).map(c => ({ ...c, tipo_origem: 'indicacoes' }));
  }
  if (tipo === 'recentes') {
    const { data, error } = await supabase.from('leads')
      .select('id, nome, telefone')
      .neq('origem', 'Indicação')
      .not('status', 'in', '("Fechou","Perdido")')
      .eq('nao_ligar', false)
      .order('criado_em', { ascending: false })
      .limit(50);
    if (error) throw error;
    return (data || []).map(c => ({ ...c, tipo_origem: 'recentes' }));
  }
  if (tipo === 'frios') {
    const { data, error } = await supabase.from('leads')
      .select('id, nome, telefone')
      .neq('origem', 'Indicação')
      .not('status', 'in', '("Fechou","Perdido")')
      .eq('nao_ligar', false)
      .order('criado_em', { ascending: false })
      .range(50, 150);
    if (error) throw error;
    return (data || []).map(c => ({ ...c, tipo_origem: 'frios' }));
  }
  const err = new Error('Tipo inválido'); err.status = 400; throw err;
}
```

- [ ] **Step 3: Atualizar `POST /api/campanhas/lancar` para aceitar `excluir`**

Dentro do handler de `POST /api/campanhas/lancar`, localizar a linha:
```js
    const contatos = await buscarContatos(tipo);
    const comTelefone = contatos.filter(c => c.telefone?.trim());
```

Substituir por:
```js
    const contatos = await buscarContatos(tipo);
    const excluirSet = new Set((req.body.excluir || []).map(String));
    const contatosFiltrados = excluirSet.size > 0
      ? contatos.filter(c => {
          const idField = tipo === 'abc' ? 'clinicorp_id' : 'id';
          return !excluirSet.has(String(c[idField]));
        })
      : contatos;
    const comTelefone = contatosFiltrados.filter(c => c.telefone?.trim());
```

- [ ] **Step 4: Commit + deploy + testar**

```bash
git add server.js
git commit -m "feat: add nao-ligar endpoint and blacklist filtering in buscarContatos"
git push
curl -s -X POST "http://2.24.94.120:3000/api/deploy/64e3f591d5f8f89c7d01ddc665d41609a5259db3bbe968e6"
```

Testar endpoint sem auth (deve retornar 401):
```bash
curl -s -o /dev/null -w "%{http_code}" -X POST "https://plataformaama-plataforma.uc5as5.easypanel.host/api/campanhas/nao-ligar"
```
Esperado: `401`

---

## Task 3: Curva ABC — Modal com Checkboxes

**Files:**
- Modify: `public/js/pos-tratamento/curva-abc.js`

Context: A função `_abrirModalCampanha` está nas linhas 287–344 de `curva-abc.js`. É um ES module (`type="module"`). Usa `backendApi(method, path, body)` para chamadas autenticadas (linhas 259–272) e `toast(msg, tipo)` importado de `./shared.js` para toasts — onde `tipo` é string `'error'` (não boolean). O campo de ID para exclusão em tipo `abc` é `c.clinicorp_id`; para outros tipos seria `c.id`, mas esta função só é chamada a partir de `lancarCampanhaABC()` com `tipo = 'abc'`.

- [ ] **Step 1: Substituir `_abrirModalCampanha` completa**

Localizar e substituir a função inteira (da linha `function _abrirModalCampanha(tipo, preview) {` até o `}` de fechamento):

```js
function _abrirModalCampanha(tipo, preview) {
  const LABELS = {
    abc: { titulo: 'Retorno ABC', sub: 'Classe A/B · Sem consulta há 180+ dias · Sem agenda' },
    indicacoes: { titulo: 'Leads Indicações', sub: 'Leads com origem = indicação · Status ativo' },
    recentes: { titulo: 'Leads Recentes', sub: 'Últimos 50 leads (não-indicação)' },
    frios: { titulo: 'Leads Frios', sub: 'Leads do 51º ao 151º (não-indicação)' },
  };
  const { titulo, sub } = LABELS[tipo] || { titulo: tipo, sub: '' };
  const isCols3 = tipo === 'abc';
  const cols = isCols3 ? ['nome', 'telefone', 'dias_sem_visita'] : ['nome', 'telefone'];
  const colLabels = isCols3 ? ['Nome', 'Telefone', 'Dias'] : ['Nome', 'Telefone'];
  const idField = tipo === 'abc' ? 'clinicorp_id' : 'id';

  const esc = s => String(s == null ? '' : s).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));

  const contatos = (preview.contatos || []).slice(0, 100);
  const total = preview.total;
  const desmarcados = new Set();
  const bloqueados = new Set();

  function _countSel() {
    return contatos.length - desmarcados.size - bloqueados.size;
  }

  function _buildRows() {
    return contatos.map(c => {
      const id = String(c[idField] || '');
      const isDes = desmarcados.has(id);
      const isBlq = bloqueados.has(id);
      const style = (isDes || isBlq) ? 'opacity:0.4' : '';
      const nuncaBtn = isDes && !isBlq
        ? `<button class="cmp-nunca" data-id="${esc(id)}" data-nome="${esc(c.nome)}" style="font-size:11px;padding:2px 6px;background:#fff5f5;color:#e53e3e;border:1px solid #fed7d7;border-radius:4px;cursor:pointer">🚫 Nunca ligar</button>`
        : '';
      const badge = isBlq ? `<span style="font-size:11px;color:#718096;background:#edf2f7;padding:2px 6px;border-radius:4px">Bloqueado</span>` : '';
      return `<tr style="${style}">
        <td style="padding:6px 8px;border-bottom:1px solid var(--color-border,#e2e8f0)">
          <input type="checkbox" class="cmp-chk" data-id="${esc(id)}" ${isBlq ? 'disabled' : ''} ${isDes || isBlq ? '' : 'checked'}>
        </td>
        ${cols.map(k => `<td style="padding:6px 8px;border-bottom:1px solid var(--color-border,#e2e8f0);font-size:13px">${esc(c[k]) || '—'}</td>`).join('')}
        <td style="padding:6px 8px;border-bottom:1px solid var(--color-border,#e2e8f0)">${nuncaBtn}${badge}</td>
      </tr>`;
    }).join('');
  }

  function _updateCounter() {
    const sel = _countSel();
    overlay.querySelector('#cmp-counter').textContent = `${sel} de ${contatos.length} selecionados`;
    const btn = overlay.querySelector('#cmp-confirmar');
    btn.disabled = sel === 0;
    btn.textContent = sel === 0 ? 'Nenhum contato selecionado' : `📞 Enviar ${sel} para discagem`;
  }

  function _rebuildTable() {
    overlay.querySelector('#cmp-tbody').innerHTML = _buildRows();
    _bindRowEvents();
    _updateCounter();
  }

  function _bindRowEvents() {
    overlay.querySelectorAll('.cmp-chk').forEach(chk => {
      chk.onchange = () => {
        if (!chk.checked) desmarcados.add(chk.dataset.id);
        else desmarcados.delete(chk.dataset.id);
        _rebuildTable();
      };
    });
    overlay.querySelectorAll('.cmp-nunca').forEach(btn => {
      btn.onclick = async () => {
        const { id, nome } = btn.dataset;
        if (!confirm(`Adicionar ${nome} à lista de não ligar? Esta pessoa não será incluída em nenhuma campanha futura.`)) return;
        btn.disabled = true; btn.textContent = '...';
        try {
          await backendApi('POST', '/api/campanhas/nao-ligar', { tipo: tipo === 'abc' ? 'paciente' : 'lead', id });
          bloqueados.add(id);
          desmarcados.delete(id); // evita dupla subtração em _countSel()
          _rebuildTable();
        } catch (e) {
          btn.disabled = false; btn.textContent = '🚫 Nunca ligar';
          toast(e.message || 'Erro ao salvar.', 'error');
        }
      };
    });
  }

  const avisoExtra = total > 100
    ? `<p style="font-size:12px;color:#718096;margin:8px 0 0;padding:8px;background:#fffbeb;border-radius:6px;border:1px solid #fbd38d">ℹ️ Mostrando os primeiros 100 de ${total}. Os demais serão incluídos automaticamente. Use 🚫 Nunca ligar para excluí-los permanentemente.</p>`
    : '';

  const overlay = document.createElement('div');
  overlay.className = 'modal-overlay';
  overlay.innerHTML = `
    <div class="modal" style="max-width:620px;max-height:85vh;display:flex;flex-direction:column">
      <h3 class="modal__title">${esc(titulo)} — ${total} contatos</h3>
      <p style="font-size:13px;color:var(--color-text-muted,#718096);margin:0 0 8px">${esc(sub)}</p>
      <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:8px">
        <label style="font-size:12px;cursor:pointer;display:flex;align-items:center;gap:6px">
          <input type="checkbox" id="cmp-selectall" checked> Selecionar todos
        </label>
        <span id="cmp-counter" style="font-size:12px;color:#718096"></span>
      </div>
      <div style="overflow-y:auto;flex:1;border:1px solid var(--color-border,#e2e8f0);border-radius:8px">
        <table style="width:100%;border-collapse:collapse">
          <thead><tr>
            <th style="width:32px;padding:8px;background:var(--color-surface-alt,#f7fafc);border-bottom:2px solid var(--color-border,#e2e8f0)"></th>
            ${colLabels.map(l => `<th style="padding:8px;text-align:left;font-size:12px;background:var(--color-surface-alt,#f7fafc);border-bottom:2px solid var(--color-border,#e2e8f0)">${l}</th>`).join('')}
            <th style="padding:8px;background:var(--color-surface-alt,#f7fafc);border-bottom:2px solid var(--color-border,#e2e8f0)"></th>
          </tr></thead>
          <tbody id="cmp-tbody"></tbody>
        </table>
      </div>
      ${avisoExtra}
      <div class="modal__actions" style="margin-top:12px;display:flex;justify-content:flex-end;gap:8px">
        <button id="cmp-cancelar" class="btn btn--ghost">Cancelar</button>
        <button id="cmp-confirmar" class="btn btn--success">📞 Enviar ${contatos.length} para discagem</button>
      </div>
    </div>`;
  document.body.appendChild(overlay);

  _rebuildTable();

  overlay.querySelector('#cmp-selectall').onchange = e => {
    if (e.target.checked) desmarcados.clear();
    else contatos.forEach(c => { const id = String(c[idField]||''); if (!bloqueados.has(id)) desmarcados.add(id); });
    _rebuildTable();
  };
  overlay.querySelector('#cmp-cancelar').onclick = () => overlay.remove();
  overlay.querySelector('#cmp-confirmar').onclick = async () => {
    const btn = overlay.querySelector('#cmp-confirmar');
    const excluir = [...desmarcados];
    const sel = _countSel();
    btn.disabled = true; btn.textContent = 'Enviando...';
    try {
      await backendApi('POST', '/api/campanhas/lancar', { tipo, excluir });
      overlay.remove();
      toast(`Campanha iniciada! ${sel} contatos na fila de discagem.`);
      if (window.campanhaWidgetRefresh) window.campanhaWidgetRefresh();
    } catch (e) {
      btn.disabled = false; btn.textContent = `📞 Enviar ${sel} para discagem`;
      toast(e.message, 'error');
    }
  };
}
```

- [ ] **Step 2: Commit**

```bash
git add public/js/pos-tratamento/curva-abc.js
git commit -m "feat: add checkboxes and nao-ligar button to curva-abc campaign modal"
```

---

## Task 4: Leads (index.html) — Modal com Checkboxes + Deploy

**Files:**
- Modify: `public/index.html`

Context: A função `_abrirModalCampanhaLeads` está nas linhas 5069–5123 de `index.html`. O helper `_esc` está na linha 5064. Usa `api(url, opts)` para chamadas autenticadas — `api('/api/campanhas/nao-ligar', { method: 'POST', body: JSON.stringify({...}) })`. Para toasts de erro usa `toast(msg, true)` com boolean (não string). O ID de exclusão para leads é sempre `c.id` (UUID). Os tipos leads são `indicacoes`, `recentes`, `frios`.

- [ ] **Step 1: Substituir `_abrirModalCampanhaLeads` completa**

Localizar e substituir a função inteira (da linha `function _abrirModalCampanhaLeads(tipo, preview) {` até o `}` de fechamento):

```js
function _abrirModalCampanhaLeads(tipo, preview) {
  const { titulo, sub } = CAMP_LABELS[tipo] || { titulo: tipo, sub: '' };
  const safeTitle = _esc(titulo);
  const safeSub = _esc(sub);
  const contatos = (preview.contatos || []).slice(0, 100);
  const total = preview.total;
  const desmarcados = new Set();
  const bloqueados = new Set();

  function _countSel() {
    return contatos.length - desmarcados.size - bloqueados.size;
  }

  function _buildRows() {
    return contatos.map(c => {
      const id = String(c.id || '');
      const isDes = desmarcados.has(id);
      const isBlq = bloqueados.has(id);
      const style = (isDes || isBlq) ? 'opacity:0.4' : '';
      const nuncaBtn = isDes && !isBlq
        ? `<button class="cml-nunca" data-id="${_esc(id)}" data-nome="${_esc(c.nome)}" style="font-size:11px;padding:2px 6px;background:#fff5f5;color:#e53e3e;border:1px solid #fed7d7;border-radius:4px;cursor:pointer">🚫 Nunca ligar</button>`
        : '';
      const badge = isBlq ? `<span style="font-size:11px;color:#718096;background:#edf2f7;padding:2px 6px;border-radius:4px">Bloqueado</span>` : '';
      return `<tr style="${style}">
        <td style="padding:6px 10px;border-bottom:1px solid var(--border)">
          <input type="checkbox" class="cml-chk" data-id="${_esc(id)}" ${isBlq ? 'disabled' : ''} ${isDes || isBlq ? '' : 'checked'}>
        </td>
        <td style="padding:6px 10px;border-bottom:1px solid var(--border);font-size:13px">${_esc(c.nome)}</td>
        <td style="padding:6px 10px;border-bottom:1px solid var(--border);font-size:13px">${_esc(c.telefone)}</td>
        <td style="padding:6px 10px;border-bottom:1px solid var(--border)">${nuncaBtn}${badge}</td>
      </tr>`;
    }).join('');
  }

  function _updateCounter() {
    const sel = _countSel();
    bg.querySelector('#cml-counter').textContent = `${sel} de ${contatos.length} selecionados`;
    const btn = bg.querySelector('#cml-confirmar');
    btn.disabled = sel === 0;
    btn.textContent = sel === 0 ? 'Nenhum contato selecionado' : `📞 Enviar ${sel} para discagem`;
  }

  function _rebuildTable() {
    bg.querySelector('#cml-tbody').innerHTML = _buildRows();
    _bindRowEvents();
    _updateCounter();
  }

  function _bindRowEvents() {
    bg.querySelectorAll('.cml-chk').forEach(chk => {
      chk.onchange = () => {
        if (!chk.checked) desmarcados.add(chk.dataset.id);
        else desmarcados.delete(chk.dataset.id);
        _rebuildTable();
      };
    });
    bg.querySelectorAll('.cml-nunca').forEach(btn => {
      btn.onclick = async () => {
        const { id, nome } = btn.dataset;
        if (!confirm(`Adicionar ${nome} à lista de não ligar? Esta pessoa não será incluída em nenhuma campanha futura.`)) return;
        btn.disabled = true; btn.textContent = '...';
        try {
          const result = await api('/api/campanhas/nao-ligar', { method: 'POST', body: JSON.stringify({ tipo: 'lead', id }) });
          if (result?.error) throw new Error(result.error); // api() não lança em erros JSON
          bloqueados.add(id);
          desmarcados.delete(id); // evita dupla subtração em _countSel()
          _rebuildTable();
        } catch (e) {
          btn.disabled = false; btn.textContent = '🚫 Nunca ligar';
          toast(e.message || 'Erro ao salvar.', true);
        }
      };
    });
  }

  const avisoExtra = total > 100
    ? `<p style="font-size:12px;color:#718096;margin:8px 0 0;padding:8px;background:#fffbeb;border-radius:6px;border:1px solid #fbd38d">ℹ️ Mostrando os primeiros 100 de ${total}. Os demais serão incluídos automaticamente. Use 🚫 Nunca ligar para excluí-los permanentemente.</p>`
    : '';

  const bg = document.createElement('div');
  bg.style.cssText = 'position:fixed;inset:0;background:rgba(0,0,0,.5);display:flex;align-items:center;justify-content:center;z-index:2000';
  bg.innerHTML = `
    <div class="modal" style="max-width:540px;max-height:85vh;display:flex;flex-direction:column">
      <div class="modal-header">
        <span class="modal-titulo">${safeTitle} — ${total} leads</span>
        <button class="modal-close" id="cml-close">×</button>
      </div>
      <p style="font-size:12px;color:var(--muted);margin:0 0 8px">${safeSub}</p>
      <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:8px">
        <label style="font-size:12px;cursor:pointer;display:flex;align-items:center;gap:6px">
          <input type="checkbox" id="cml-selectall" checked> Selecionar todos
        </label>
        <span id="cml-counter" style="font-size:12px;color:#718096"></span>
      </div>
      <div style="overflow-y:auto;flex:1;border:1px solid var(--border);border-radius:8px">
        <table style="width:100%;border-collapse:collapse">
          <thead><tr>
            <th style="width:32px;padding:8px 10px;background:#f7fafc;border-bottom:2px solid var(--border)"></th>
            <th style="padding:8px 10px;text-align:left;font-size:12px;background:#f7fafc;border-bottom:2px solid var(--border)">Nome</th>
            <th style="padding:8px 10px;text-align:left;font-size:12px;background:#f7fafc;border-bottom:2px solid var(--border)">Telefone</th>
            <th style="padding:8px 10px;background:#f7fafc;border-bottom:2px solid var(--border)"></th>
          </tr></thead>
          <tbody id="cml-tbody"></tbody>
        </table>
      </div>
      ${avisoExtra}
      <div style="margin-top:12px;display:flex;justify-content:flex-end;gap:8px">
        <button id="cml-cancelar" class="btn" style="background:#edf2f7;color:#4a5568">Cancelar</button>
        <button id="cml-confirmar" class="btn btn-primary">📞 Enviar ${contatos.length} para discagem</button>
      </div>
    </div>`;
  document.body.appendChild(bg);

  _rebuildTable();

  bg.querySelector('#cml-selectall').onchange = e => {
    if (e.target.checked) desmarcados.clear();
    else contatos.forEach(c => { const id = String(c.id||''); if (!bloqueados.has(id)) desmarcados.add(id); });
    _rebuildTable();
  };
  bg.querySelector('#cml-close').onclick = () => bg.remove();
  bg.querySelector('#cml-cancelar').onclick = () => bg.remove();
  bg.querySelector('#cml-confirmar').onclick = async () => {
    const btn = bg.querySelector('#cml-confirmar');
    const excluir = [...desmarcados];
    const sel = _countSel();
    btn.disabled = true; btn.textContent = 'Enviando...';
    try {
      const launchResult = await api('/api/campanhas/lancar', { method: 'POST', body: JSON.stringify({ tipo, excluir }) });
      if (launchResult?.error) throw new Error(launchResult.error); // api() não lança em erros JSON
      bg.remove();
      toast(`Campanha iniciada! ${sel} leads na fila de discagem.`);
      if (window.campanhaWidgetRefresh) window.campanhaWidgetRefresh();
    } catch (e) {
      btn.disabled = false; btn.textContent = `📞 Enviar ${sel} para discagem`;
      toast(e.message || 'Erro ao lançar campanha.', true);
    }
  };
}
```

- [ ] **Step 2: Commit + deploy**

```bash
git add public/index.html public/js/pos-tratamento/curva-abc.js
git commit -m "feat: add checkboxes and nao-ligar button to campaign preview modals"
git push
curl -s -X POST "http://2.24.94.120:3000/api/deploy/64e3f591d5f8f89c7d01ddc665d41609a5259db3bbe968e6"
```

Verificar no browser:
1. Módulo Leads → clicar "📞 Indicações" → modal abre com checkboxes, contador "X de N selecionados"
2. Desmarcar alguém → linha fica cinza + botão "🚫 Nunca ligar" aparece
3. Botão "📞 Enviar X para discagem" atualiza o número dinamicamente
4. Curva ABC → mesmo comportamento com coluna "Dias" adicional
